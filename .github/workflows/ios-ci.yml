# iOS CI — builds the SwiftUI app (MobileGarage/iosApp) against the Kotlin
# shared.framework on a macOS runner, and runs the iosFramework DI-graph
# identity test (NativeComponentTest).
#
# NOT a required status check (yet). It validates the iOS build going forward;
# branch protection still gates on "Android CI Complete" / "Firebase CI Complete".
# Promote to required once it has a stable green history (see CLAUDE.md
# "Renaming a branch-protection-required gate job" for the ordering rules).
#
# Scoped to the modules the framework actually pulls in, so Android-only PRs
# don't pay for a macOS runner.

name: iOS CI

on:
  push:
    branches: [main]
    paths:
      - 'MobileGarage/iosApp/**'
      - 'MobileGarage/iosFramework/**'
      - 'MobileGarage/domain/**'
      - 'MobileGarage/data/**'
      - 'MobileGarage/data-local/**'
      - 'MobileGarage/usecase/**'
      - 'MobileGarage/viewmodel/**'
      - 'MobileGarage/presentation-model/**'
      - 'MobileGarage/gradle/libs.versions.toml'
      - 'MobileGarage/settings.gradle.kts'
      - 'MobileGarage/build.gradle.kts'
      - '.github/workflows/ios-ci.yml'
  pull_request:
    paths:
      - 'MobileGarage/iosApp/**'
      - 'MobileGarage/iosFramework/**'
      - 'MobileGarage/domain/**'
      - 'MobileGarage/data/**'
      - 'MobileGarage/data-local/**'
      - 'MobileGarage/usecase/**'
      - 'MobileGarage/viewmodel/**'
      - 'MobileGarage/presentation-model/**'
      - 'MobileGarage/gradle/libs.versions.toml'
      - 'MobileGarage/settings.gradle.kts'
      - 'MobileGarage/build.gradle.kts'
      - '.github/workflows/ios-ci.yml'

concurrency:
  group: ios-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-ios:
    name: Build iOS app + framework test
    # Pinned to macos-26 (default Xcode 26.5) to match the release archive's SDK.
    # Apple requires the iOS 26 SDK (Xcode 26+) for uploads; macos-latest still
    # resolved to Xcode 16.4 in mid-2026. See docs/IOS_RELEASE_SETUP.md.
    runs-on: macos-26
    timeout-minutes: 40
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-ios-${{ hashFiles('MobileGarage/**/*.gradle*', 'MobileGarage/gradle/libs.versions.toml') }}
          restore-keys: |
            ${{ runner.os }}-gradle-ios-
            ${{ runner.os }}-gradle-

      - name: Cache Xcode DerivedData
        uses: actions/cache@v4
        with:
          path: ~/Library/Developer/Xcode/DerivedData
          key: ${{ runner.os }}-xcode-ios-${{ hashFiles('MobileGarage/iosApp/**/*.swift', 'MobileGarage/iosApp/project.yml') }}
          restore-keys: |
            ${{ runner.os }}-xcode-ios-

      - name: Install XcodeGen
        run: brew install xcodegen

      # DI-graph identity test (mirrors NativeComponentTest, 40/40 on simulator).
      # Also warms the framework artifacts the app build embeds.
      - name: Run iosFramework simulator tests
        working-directory: MobileGarage
        run: ./gradlew :iosFramework:iosSimulatorArm64Test

      - name: Generate Xcode project
        working-directory: MobileGarage/iosApp
        run: xcodegen generate

      - name: Build iOS app (simulator)
        working-directory: MobileGarage/iosApp
        run: |
          xcodebuild \
            -project GarageControl.xcodeproj \
            -scheme GarageControl \
            -configuration Debug \
            -sdk iphonesimulator \
            -destination 'generic/platform=iOS Simulator' \
            build \
            CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO CODE_SIGNING_ALLOWED=NO
