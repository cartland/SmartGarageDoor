---
category: plan
status: active
last_verified: 2026-05-11
---

# Pending Smoke Tests

User-facing changes that have shipped to the **internal Play Store track** but not yet been smoke-tested on a real device. Each item is one cohesive scenario to verify on hardware.

**Scope of this list:** Android-only. Server-side changes that ride along with an Android release (e.g. server pubsub cadence) are noted on the Android item that depends on them.

**Workflow:**

1. When a release ships to internal track, append a row here with the version, the scenario to verify, and a short reproduction.
2. When the user smoke-tests the device, they (or the agent on request) checks items off and removes them from this list.
3. If a smoke test fails, file a GitHub issue and reference it here; remove from this list once the issue is filed (don't dual-track).
4. Items roll up by user-facing version, not by every patch — if 2.16.10 and 2.16.11 ship two versions of the same scenario, list the latest one with a note covering both.

**Why this lives in the repo, not memory:** the smoke queue is project-specific TODO state. Agents should not write it to memory (see `feedback_dump_context_repo_first.md`); the user maintains it across sessions and across machines.

## Cumulative queue

1. **`android/230` / 2.16.16 — Home notification-permission banner copy.** When the app starts in a state where notification permission has not been granted, the Home banner now reads *"Turn on notifications to get alerted when the door is left open."* (was *"Please turn on notifications to be notified when the door is left open."*). Verify: (a) banner is 2 lines, not 3; (b) tapping the banner still launches the system permission prompt; (c) at attempt-count 3+ the appended hint *"You can manage permissions in the Android system settings."* still reads naturally below the new lead. To trigger: deny notification permission (or freshly install on Android 13+ before granting), open the app — banner is on Home above the door card.
2. **`android/231` / 2.16.17 — Diagnostics "Copy auth token (sensitive)" button.** New OutlinedButton at the top of the Diagnostics action stack (Settings → Developer → Diagnostics) on Android 13+. To verify: (a) on a phone running Android 13+, the button appears above Export CSV with the key icon and label "Copy auth token (sensitive)"; (b) tapping it while signed in shows the OS clipboard preview chip with the token content **redacted** (not the raw JWT); (c) pasting into a text field produces the full JWT; (d) tapping while signed out shows Toast "Sign in to copy auth token"; (e) **bonus verification of `server/26` `developerAccess`**: paste into `curl -H "X-AuthTokenGoogle: <pasted>" "https://us-central1-escape-echo.cloudfunctions.net/developerAccess"` — should return `{"enabled":true}` for an email in `featureDeveloperAllowedEmails`. On a device running Android 12 or older, the button must be **hidden entirely** (no leak path through the OS preview chip).
3. **`android/232` / 2.16.18 — Diagnostics action buttons scroll with counters.** The action stack (Copy auth token / Export CSV / Clear all) is no longer pinned to the bottom of the viewport — it scrolls with the counters surface above. To verify: open Settings → Developer → Diagnostics on a device with a tall counters list (or in landscape so the viewport is short); confirm (a) flicking up scrolls the counters AND the buttons together, (b) the last button is reachable above the gesture nav once scrolled into view (the `safeListContentPadding` bottom band is the safety margin), (c) Compact / Wide / Expanded layouts all behave the same way (no mode-specific regression).
4. **`android/233` / 2.16.19 — Function List "Copy auth token (sensitive)" button + shared UseCase.** Same redaction-flag behavior as the Diagnostics surface, now also on Settings → Developer → Function list (bottom of the list). To verify: (a) the button appears at the bottom of the Function List on Android 13+; (b) tapping copies the **same token** that the Diagnostics button copies (sanity-check by pasting both into a diff tool — they should match because both call sites now share `GetAuthTokenForCopyUseCase`); (c) the OS preview chip redacts the content on both surfaces; (d) hidden entirely on API < 33. **Verification property:** any regression in the token-fetch / clipboard / redaction path now breaks both screens at once — if Diagnostics works but Function List doesn't (or vice versa), there's a bug in the call-site wiring of `rememberAuthTokenCopier()`, not in the shared logic.
5. **`android/234` / 2.16.20 — Settings → Developer → "Layout debug colors" toggle.** New developer-only toggle that paints the TopAppBar magenta, the bottom NavigationBar / left NavigationRail cyan, and the Scaffold body pale yellow when on. Persisted in DataStore (`LAYOUT_DEBUG_ENABLED`) so it survives process death. The toggle row itself is gated behind the developer-allowlist (`functionListAccess` in this release; switched to dedicated `developerAccess` in `android/235` below). To verify: (a) toggle row visible only when developer-allowlisted; (b) tap toggles immediately AND persists across app restart (kill app → reopen → state preserved); (c) when on, all three chrome regions paint distinct debug colors and the chrome-vs-body boundary becomes obvious; (d) when off, chrome reverts to Material defaults with no residual tint; (e) check on Compact (bottom nav cyan), Wide (left rail cyan), Expanded (no nav chrome — only TopAppBar + body get tinted).
6. **`android/235` / 2.16.21 — Settings → Developer gated by dedicated `developerAccess` allowlist.** The whole section now reads from the new server endpoint (`server/26`) instead of piggybacking on `functionListAccess`. Four-quadrant matrix to verify on a real device by toggling email membership in the Firestore `featureDeveloperAllowedEmails` and `featureFunctionListAllowedEmails` arrays: (a) account on **both** allowlists → section visible WITH Function-list row inside; (b) account on `featureDeveloperAllowedEmails` but NOT `featureFunctionListAllowedEmails` → section visible (Diagnostics + Layout debug colors), Function-list row HIDDEN; (c) account on `featureFunctionListAllowedEmails` but NOT `featureDeveloperAllowedEmails` → section HIDDEN entirely; (d) account on neither → section HIDDEN. Sign-out on any of these clears the cache to null; section disappears immediately. **Server side already verified end-to-end via curl** with a real ID token earlier this session — `GET /developerAccess` returned `{"enabled":true}` for `c.cartland@gmail.com` and `401` without a token, so any failure here is in the Android wire-up, not the contract.

## Open follow-ups (release-related but not smoke-test items)

- **Whatsnew accuracy.** `AndroidGarage/distribution/whatsnew/whatsnew-en-US` is currently triple-stale as of 2.16.11+:
  1. Says "side by side at 840dp+" but the threshold has been 1200dp since 2.16.7.
  2. Doesn't mention the left-rail in Wide mode (added 2.16.10).
  3. Doesn't mention the rail items being centered (changed 2.16.11).
  Per the `bump-android-version` skill, **patches don't touch whatsnew** — patches roll up into the next minor/major. Stays out of sync until a 2.17.x bump or until the user explicitly OKs an exception.
- **Other open follow-ups (user-visible-string migration)** are tracked in [`PENDING_FOLLOWUPS.md`](./PENDING_FOLLOWUPS.md). Both server (`server/26`, 2026-05-10) and Android (`android/235`, 2026-05-11) sides of the Developer-allowlist flag have shipped.
