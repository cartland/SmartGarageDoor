---
pr: 16
state: merged
date: 2026-04-03
phase1_reviewed: 2026-04-20
---

# PR #16: ci: Add validation script, Claude hooks, and fix CI path filtering

**Goal:** `scripts/validate.sh` — local CI mirror (spotless, 3 test variants, debug build). Writes validation marker. Claude hooks: `block-admin-bypass` (denies `--admin` on PR merge), `git-guardrails` (blocks push to main, force push, destructive commands, enforces `--squash`). CI fix: remove `paths:` filter from ci.yml so required checks always report status.

**Files:** `scripts/validate.sh` (new, 37 lines), `.claude/hooks/block-admin-bypass.sh` (new), `git-guardrails.sh` (new, 81 lines), `.claude/settings.json`, `.github/workflows/ci.yml`, `.gitignore`.

## Phase 2 assessment

**Primary:** great
**Secondary:** []
**Phase 2 reviewed:** 2026-04-20

**Rationale:** Foundational safety infrastructure — `validate.sh` + `block-admin-bypass` + `git-guardrails` all still load-bearing.

**Still-current lesson:** Local validation script + hooks are the baseline safety net. `validate.sh` mirrors CI (no surprise failures after push); hooks catch destructive commands and `--admin` bypass before they happen.
