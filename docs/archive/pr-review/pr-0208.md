---
pr: 208
state: merged
date: 2026-04-07
phase1_reviewed: 2026-04-20
---

# PR #208: test: Add state restoration tests and rememberSaveable guard

**Goal:** `RememberSaveableGuardTask` (architecture check, CI) blocks `rememberSaveable` without explicit `saver` — prevents the crash that shipped in android/140. `ExpandableColumnCardContractTest` verifies Screen objects aren't `java.io.Serializable`. `StateRestorationTest` (instrumented) tests ExpandableColumnCard syncs external state, fires callbacks, survives process death. `ConfigurationChangeTest` tests all screens survive Activity recreation.

**Files:** `AndroidGarage/androidApp/src/androidTest/.../ConfigurationChangeTest.kt` (new), `StateRestorationTest.kt` (new), `ExpandableColumnCardContractTest.kt` (new), `buildSrc/.../RememberSaveableGuardTask.kt` (new), `build.gradle.kts`, `scripts/validate.sh`.

## Phase 2 assessment

**Primary:** great
**Secondary:** []
**Phase 2 reviewed:** 2026-04-20

**Rationale:** Lint + contract test + instrumented test trio as a direct response to the android/140 crash — exactly the defense-in-depth pattern for invisible bugs.

**Still-current lesson:** `rememberSaveable` without an explicit `Saver` is a latent crash waiting for process death — lint for it, contract-test the types you save, instrumented-test the screens survive recreation.
