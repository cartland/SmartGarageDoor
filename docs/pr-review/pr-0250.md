---
pr: 250
state: merged
date: 2026-04-09
phase1_reviewed: 2026-04-20
---

# PR #250: refactor: RemoteButtonViewModel uses UseCases (Phase 43 complete)

**Goal:** Phase 43 complete — last ViewModel migrated. `RemoteButtonViewModel` no longer imports any repository directly. New `ObservePushButtonStatusUseCase` wraps `remoteButtonRepository.pushButtonStatus`. `ObserveDoorEventsUseCase` gains a `position()` method. `ButtonStateMachine` constructed with UseCase flows. `listenToDoorEvent` uses `observeDoorEvents.current()`. All 5 ViewModels (AppLogger, AppSettings, Auth, Door, RemoteButton) depend only on UseCases and domain types.

**Files:** `AndroidGarage/usecase/.../RemoteButtonViewModel.kt`, `ObserveDoorEventsUseCase.kt`, `ObservePushButtonStatusUseCase.kt` (new), `RemoteButtonViewModelTest.kt`, `AppComponent.kt`.
