---
pr: 216
state: merged
date: 2026-04-08
phase1_reviewed: 2026-04-20
---

# PR #216: refactor: Inject AppConfig via DI, remove hardcoded URLs

**Goal:** Base URL → `BuildConfig.BASE_URL` (set in build.gradle.kts). `APP_CONFIG` global singleton → `AppConfigFactory.create()` injected via `provideAppConfig()` in AppComponent. Remove unused enums: `Server`, `InitialData`, `FetchOnViewModelInit`. Feature flags passed as composable parameters. Rename `LocalConfig.kt` → `AppConfigFactory.kt`.

**Files:** `AndroidGarage/androidApp/build.gradle.kts`, `config/AppConfigFactory.kt` (rename from `LocalConfig.kt`), `AppComponent.kt`, `ProfileContent.kt`, `AppConfigFactoryTest.kt`, `domain/.../AppConfig.kt`.

## Phase 2 assessment

**Primary:** ok
**Secondary:** []
**Phase 2 reviewed:** 2026-04-20

**Rationale:** Removed a global singleton + hardcoded URL; AppConfig now passes cleanly through DI.
