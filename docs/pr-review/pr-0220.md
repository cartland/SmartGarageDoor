---
pr: 220
state: merged
date: 2026-04-08
phase1_reviewed: 2026-04-20
---

# PR #220: refactor: Move remaining pure Kotlin tests to shared modules

**Goal:** Move `LoadingResultTest`, `DoorFcmModelTest`, `FcmTopicTest` → `domain/commonTest`. Move `FcmPayloadParsingTest` → `data/commonTest`. Delete duplicate `ServerConfigRepositoryTest` (covered by data module). androidApp now has only 7 test files, all genuinely Android-specific. Net -192 lines.

**Files:** delete `androidApp/.../ServerConfigRepositoryTest.kt`, `data/commonTest/.../FcmPayloadParsingTest.kt`, `domain/commonTest/.../DoorFcmModelTest.kt`, `FcmTopicTest.kt`, `LoadingResultTest.kt`.

## Phase 2 assessment

**Primary:** great
**Secondary:** []
**Phase 2 reviewed:** 2026-04-20

**Rationale:** Tests live in the module they test; pure-Kotlin tests in `commonTest` so they'll run on every KMP target. androidApp retains only genuinely Android-specific tests.

**Still-current lesson:** "Where does the test live" matches "where does the code live" in a KMP project — `commonMain` code gets `commonTest` tests; platform-only code gets platform-only tests.
