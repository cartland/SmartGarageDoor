---
pr: 40
state: merged
date: 2026-04-03
phase1_reviewed: 2026-04-20
---

# PR #40: feat: Add Android release script with tag safety guardrails

**Goal:** Add `scripts/release-android.sh` for safe Android releases. Modes: interactive, `--check`, `--confirm-tag android/N`, `--dry-run`. Safety: computes next tag as `android/<highest + 1>` (cannot be overridden); `--confirm-tag` must match computed tag; requires clean tree, CI passing, main branch. `.claude/hooks/git-guardrails.sh` blocks `git tag` (only `git tag -l` allowed).

**Files:** `scripts/release-android.sh` (new, 255 lines), `.claude/hooks/git-guardrails.sh`, `.github/actions/setup-android/action.yml` (new), `.github/workflows/ci.yml`, `release-android.yml` (new, 131 lines), `CLAUDE.md`, `androidApp/build.gradle.kts`.

## Phase 2 assessment

**Primary:** great
**Secondary:** []
**Phase 2 reviewed:** 2026-04-20

**Rationale:** Release infra with tag-collision safety + hook-enforced "no direct `git tag`" — foundational.

**Still-current lesson:** Release script computes the next tag (never takes a user override) + a hook blocks direct `git tag` — two-layer guard against cutting the wrong version.
