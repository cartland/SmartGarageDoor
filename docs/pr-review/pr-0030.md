---
pr: 30
state: merged
date: 2026-04-03
phase1_reviewed: 2026-04-20
---

# PR #30: test: Add shared fake implementations for repositories

**Goal:** Add reusable fake implementations in `testcommon/` package. `FakeDoorRepository` (MutableStateFlows, tracks fetch counts). `FakePushRepository` (tracks push/snooze calls, controllable status flows). `FakeAuthRepository` (controllable auth state, tracks call counts). Replace Mockito mocks with readable, KMP-compatible fakes (ADR-003). Phase 1.3.

**Files:** `AndroidGarage/androidApp/src/test/.../testcommon/FakeAuthRepository.kt` (new), `FakeDoorRepository.kt` (new), `FakePushRepository.kt` (new).
