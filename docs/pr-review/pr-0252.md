---
pr: 252
state: merged
date: 2026-04-09
phase1_reviewed: 2026-04-20
---

# PR #252: feat: Enforce ViewModel→UseCase only via LayerImportCheck (Phase 43 complete)

**Goal:** Phase 43 complete. Add `com.chriscartland.garage.domain.repository.` to `LayerImportCheckTask` forbidden imports for files matching `.*ViewModel\.kt`. Any future ViewModel that tries to import a domain repository interface will fail with: "ViewModels must depend on UseCases, not domain repository interfaces. Wrap repository access in a UseCase." Matches battery-butler's pattern — keeps dependency graph clean, forces business logic into UseCases, enables future `:viewmodel` module separation.

**Files:** `AndroidGarage/build.gradle.kts`, `AndroidGarage/docs/TESTING.md`.

## Phase 2 assessment

**Primary:** great
**Secondary:** []
**Phase 2 reviewed:** 2026-04-20

**Rationale:** Banning `domain.repository.*` imports inside ViewModels is *the* load-bearing rule that keeps business logic in UseCases and enables future module splits. Still the rule; still the right rule.

**Still-current lesson:** When you finish a "X may only depend on Y" migration, land the lint that enforces the invariant in the same wave. Conventions without enforcement decay one convenient import at a time.
