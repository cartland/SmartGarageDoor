<?xml version="1.0" encoding="utf-8"?>
<!--
  ~ Copyright 2024 Chris Cartland. All rights reserved.
  ~
  ~ Licensed under the Apache License, Version 2.0 (the "License");
  ~ you may not use this file except in compliance with the License.
  ~ You may obtain a copy of the License at
  ~
  ~     http://www.apache.org/licenses/LICENSE-2.0
  ~
  ~ Unless required by applicable law or agreed to in writing, software
  ~ distributed under the License is distributed on an "AS IS" BASIS,
  ~ WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  ~ See the License for the specific language governing permissions and
  ~ limitations under the License.
  ~
  -->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".GarageApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Garage">

        <profileable
            android:shell="true"
            tools:targetApi="29" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".fcm.FCMService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Wear auth relay: answers the watch's RPC for the phone's
             signed-in identity + a Firebase ID token (Wearable Data Layer).
             Play services starts and binds it on demand; the path filter
             matches WearAuthRelayProtocol.ID_TOKEN_PATH. exported=true is
             the documented WearableListenerService pattern (only Play
             services can deliver these intents). -->
        <service
            android:name=".wearrelay.WearAuthRelayService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.REQUEST_RECEIVED" />
                <data
                    android:host="*"
                    android:pathPrefix="/garage/auth"
                    android:scheme="wear" />
            </intent-filter>
        </service>

        <!--
          FCM: render OS-shown (background) open-door warnings on the app-owned
          "Garage door" channel + garage icon, instead of FCM's default
          "Miscellaneous" channel + launcher icon (M4). The channel id is the
          shared @string/door_notification_channel_id (also used by
          DoorNotificationPresenter); the channel itself is created eagerly at
          startup (GarageApplication.onCreate) so it exists before any
          background warning arrives.
        -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/door_notification_channel_id" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification_garage" />
    </application>

</manifest>